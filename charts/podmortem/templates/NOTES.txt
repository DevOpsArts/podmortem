Podmortem has been deployed to namespace {{ .Release.Namespace }}.

To check the watcher status:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "podmortem.name" . }}
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "podmortem.name" . }} -f

{{- if .Values.watchNamespace }}
Watching namespace: {{ .Values.watchNamespace }}
{{- else }}
Watching: all namespaces
{{- end }}

To query restart history locally (port-forward not needed — exec into the pod):

  POD=$(kubectl get pod -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "podmortem.name" . }} -o jsonpath='{.items[0].metadata.name}')
  kubectl exec -n {{ .Release.Namespace }} $POD -- podmortem history
  kubectl exec -n {{ .Release.Namespace }} $POD -- podmortem detail 1
